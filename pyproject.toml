[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "itatorrents"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pymediainfo>=6.0", "guessit>=3.8"]

[project.scripts]
itatorrents = "itatorrents:main"
